/// Architecture-specific implementations for addresses
pub mod addresses;

/// The virtual memory manager
pub mod memory_manager;

/// The physical page allocator
pub mod page_allocator;

/// Page tables for the virtual memory manager
pub mod tables;
